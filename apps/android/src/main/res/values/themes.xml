<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base para app 100% Compose — los colores reales los maneja Compose Material3.
         Usamos android:Theme.Material.Light.NoTitleBar (framework, sin deps externas) -->
    <style name="Theme.WakeyWakey" parent="android:Theme.Material.Light.NoTitleBar">
        <item name="android:colorPrimary">#FFE03A</item>
        <item name="android:colorAccent">#FFE03A</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
