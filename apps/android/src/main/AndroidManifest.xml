<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Calendarios -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <!-- Notificaciones (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Alarmas exactas -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Alerta full-screen (Android 14+ requiere justificación en Play Store) -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Overlay sobre otras apps (opcional, para modo sin activity) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Mantener CPU activa para alarmas -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Foreground service (para mantener alarmas vivas) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Ignorar optimización de batería (onboarding pedirá al usuario) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Relanzar alarmas tras reinicio -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Internet para Supabase / RevenueCat -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".WakeyWakeyApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.WakeyWakey"
        android:supportsRtl="true"
        android:allowBackup="false">

        <!-- Pantalla principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity de alerta full-screen (se muestra sobre la pantalla de bloqueo) -->
        <activity
            android:name=".alert.AlertActivity"
            android:exported="false"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true"
            android:taskAffinity="com.sierraespada.wakeywakey.alert" />

        <!-- BroadcastReceiver para disparar la alarma -->
        <receiver
            android:name=".alarm.AlarmReceiver"
            android:exported="false" />

        <!-- BroadcastReceiver para relanzar alarmas tras reinicio -->
        <receiver
            android:name=".alarm.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Foreground Service para mantener el scheduler activo -->
        <service
            android:name=".scheduler.SchedulerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="calendar_alert" />
        </service>

    </application>

</manifest>
